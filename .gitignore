# Rust
/target

# Local runtime config — use config.example.yaml as template
config.yaml

# Scan output and consume delivery — never commit user data
scans/
consume/

# Python (legacy, kept for main branch)
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv
src/nx_boss/_version.py
